services:
  - type: web
    name: gamehub
    env: docker
    plan: free
    
  
    envVars:
      - key: NODE_ENV
        value: production
      - key: OWNER_USERNAME
        value: parham-savar
      - key: OWNER_PASSWORD
        generateValue: true
      - key: DATA_DIR
        value: /opt/render/project/src/data
